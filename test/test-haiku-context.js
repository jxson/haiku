
const haiku = require('../')
    , assert = require('assert')
    , resolve = require('./resolve')
    , src = resolve('src')

describe('haiku-context/content template variable', function(){
  it('populates the content collection')
  it('contains pages in the --content-dir')
  it('does not include indexes')
  it('does not include drafts')
  it('does not include non-enumerables')
  it('has accessors for sub-directories')
  it('sorts descending by date')
  it('sorts by name if missing dates')
})
