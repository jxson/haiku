var helper = require('../test_helper')
  , vows = require('vows')
  , assert = require('assert')
;

vows.describe('Collection').addBatch({
  'new Collection(options)': 'pending',
  '#read()': 'pending',
  '#find() / #resolve()': 'pending',
  '#getIndex()': 'pending',
  '#collections()': 'pending',
  '#pages()': 'pending',
  '#toJSON()': 'pending'
}).export(module);
