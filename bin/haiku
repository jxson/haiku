#!/usr/bin/env node

var haiku = require('../lib/haiku/')
  , app = new haiku.App()
;

app.use(haiku.plugins.cli);

if (require.main === module) {
  app.init(function(err){
    if (err) throw err;
  });
}
